// import express from 'express';
// import RouteMascotas from './routes/mascotas';
// import RouteProductos from './routes/productos';

// const app = express();

// app.use(express.json());
// app.use(express.urlencoded({extended:true}))

// // app.use('/mascotas', require('./routes/mascotas').default)
// app.use('/api/mascotas', RouteMascotas)
// app.use('/api/productos', RouteProductos)

// app.listen(8080, () => {
//     try {
//         console.log('server is running in port 8080')
//     } catch (error) {
//         console.log('Ese puerto ya esta tomado bro!')
//     }
// })